% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/col-shortcuts.R
\name{col_shortcuts}
\alias{col_shortcuts}
\alias{col_match}
\alias{col_match_verbose}
\alias{col_match_checklist}
\alias{col_dataset}
\alias{col_dataset_metrics}
\alias{col_usage}
\alias{col_usage_search}
\alias{col_search}
\alias{col_suggest}
\alias{col_classification}
\alias{col_synonyms}
\alias{col_vernacular}
\alias{col_usage_metrics}
\alias{col_tree}
\alias{col_children}
\title{Catalogue of Life shortcuts}
\usage{
col_match(
  name,
  authorship = NULL,
  rank = NULL,
  code = NULL,
  server = NULL,
  .raw = FALSE
)

col_match_verbose(
  name,
  authorship = NULL,
  rank = NULL,
  code = NULL,
  server = NULL,
  .raw = FALSE
)

col_match_checklist(
  data,
  name = "name",
  authorship = "authorship",
  rank = "rank",
  code = "code",
  server = NULL,
  max_active = 5L
)

col_dataset(.raw = FALSE)

col_dataset_metrics(.raw = FALSE)

col_usage(id, .raw = FALSE)

col_usage_search(
  q = NULL,
  rank = NULL,
  status = NULL,
  ...,
  limit = 50L,
  max = limit
)

col_search(q = NULL, rank = NULL, status = NULL, ..., limit = 50L, max = limit)

col_suggest(q, ..., .raw = FALSE)

col_classification(id, wide = FALSE, .raw = FALSE)

col_synonyms(id, full = FALSE, .raw = FALSE)

col_vernacular(
  id = NULL,
  q = NULL,
  lang = NULL,
  ...,
  limit = 50L,
  max = limit,
  full = FALSE,
  .raw = FALSE
)

col_usage_metrics(id, .raw = FALSE)

col_tree(
  id = NULL,
  extinct = TRUE,
  ...,
  limit = 100L,
  max = limit,
  .raw = FALSE
)

col_children(id, extinct = TRUE, ..., limit = 100L, max = limit, .raw = FALSE)
}
\arguments{
\item{name, authorship, rank, code, server, data, max_active}{See \code{\link[=clb_match]{clb_match()}} and
\code{\link[=clb_match_checklist]{clb_match_checklist()}}.}

\item{.raw}{Return the raw parsed JSON instead of a tibble?}

\item{id}{Usage or taxon id within the COL release.}

\item{q}{Free-text query (or partial name for \code{\link[=col_suggest]{col_suggest()}}).}

\item{status}{Taxonomic status filter.}

\item{...}{Further query parameters forwarded to the underlying \verb{clb_*()}
function.}

\item{limit, max}{Pagination controls, as in the \verb{clb_*()} functions.}

\item{wide}{Logical; if \code{TRUE}, unnest classification hierarchy into wide columns.}

\item{full}{Logical; if \code{FALSE} (default), returns essential core columns. If \code{TRUE}, includes full metadata.}

\item{lang}{ISO language filter for \code{\link[=col_vernacular]{col_vernacular()}}.}

\item{extinct}{Logical; include extinct taxa in tree navigation.}
}
\value{
As the corresponding \verb{clb_*()} function.
}
\description{
Convenience siblings of the dataset-scoped \verb{clb_*()} functions that always
operate on the latest extended Catalogue of Life release. They take the same
arguments as their \verb{clb_*()} counterparts but without the \code{dataset}
parameter; the release is resolved once to its integer key via \code{\link[=col_key]{col_key()}}
and reused for the rest of the session (see \code{\link[=col_refresh]{col_refresh()}} to re-pin).
}
\examples{
\dontrun{
col_match("Panthera leo")
col_usage_search("Felidae")
roots <- col_tree()
col_children(roots$id[1])
col_classification("4CGXP")
}
}
\seealso{
\code{\link[=col_key]{col_key()}}, \code{\link[=clb_match]{clb_match()}}, \code{\link[=clb_usage_search]{clb_usage_search()}}, \code{\link[=clb_tree]{clb_tree()}}
}
