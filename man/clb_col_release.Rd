% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/col.R
\name{clb_col_release}
\alias{clb_col_release}
\title{Resolve a Catalogue of Life release}
\usage{
clb_col_release(cadence = c("monthly", "annual"), extended = FALSE)
}
\arguments{
\item{cadence}{Either \code{"monthly"} (the default) or \code{"annual"}.}

\item{extended}{Logical. Return the extended release (\code{XR}) instead of the
base release? Defaults to \code{FALSE}.}
}
\value{
A length-one character vector with the release alias, or \code{NA} (with
a warning) when the requested release has not been published yet -- as is
currently the case for the annual extended release.
}
\description{
The Catalogue of Life is published as a managed project (dataset key \code{3})
from which immutable \emph{releases} are derived. There are two cadences
(monthly and annual) and two flavours (a \emph{base} release and an \emph{extended}
release \code{XR}, which merges additional external data). This helper returns
the dataset identifier (an alias such as \code{"3LXR"} or \code{"COL25"}) you can pass
to any \verb{dataset =} argument in this package.
}
\details{
The monthly releases have stable magic aliases that always point at the most
recent one:
\itemize{
\item \code{"3LR"} -- latest monthly \strong{base} release
\item \code{"3LXR"} -- latest monthly \strong{extended} release
}

Annual releases use a \verb{COL<YY>} alias (e.g. \code{"COL25"}, and \code{"COL25 XR"} for
the extended variant). The latest one is discovered from the list of all
releases of project \code{3}.
}
\examples{
\dontrun{
clb_col_release("monthly")                  # "3LR"
clb_col_release("monthly", extended = TRUE) # "3LXR"
clb_col_release("annual")                   # e.g. "COL25"
}
}
\seealso{
\code{\link[=clb_col_releases]{clb_col_releases()}} for the full list of releases.
}
