% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{clb_extract_classification}
\alias{clb_extract_classification}
\alias{col_extract_classification}
\title{Extract taxonomic classification from a match or usage result}
\usage{
clb_extract_classification(data, wide = FALSE, dataset = "3LXR", ...)

col_extract_classification(data, wide = FALSE, dataset = "3LXR", ...)
}
\arguments{
\item{data}{A character vector of scientific names, or a data frame / tibble
returned by a matching or usage function that contains a \code{classification}
list-column.}

\item{wide}{Logical. If \code{TRUE}, pivots the major ranks (\code{kingdom}, \code{phylum},
\code{class}, \code{order}, \code{family}, \code{genus}, \code{species}) into columns alongside each
input row. If \code{FALSE} (the default), returns a long tibble of all
classification levels.}

\item{dataset}{Dataset key or alias used when \code{data} is a character vector.
Defaults to \code{"3LXR"}.}

\item{...}{Further query parameters passed to \code{\link[=clb_match]{clb_match()}} or
\code{\link[=clb_match_checklist]{clb_match_checklist()}} when \code{data} is a character vector.}
}
\value{
A \link[tibble:tibble]{tibble}.
}
\description{
Helper function to extract and unnest the nested \code{classification} list-column
returned by \code{\link[=clb_match]{clb_match()}}, \code{\link[=clb_match_checklist]{clb_match_checklist()}}, \code{\link[=col_match]{col_match()}}, or
\code{\link[=clb_usage_search]{clb_usage_search()}} into a tidy long or wide tibble. Also accepts a character
vector of scientific names directly.
}
\examples{
\dontrun{
# Pass a character string directly:
col_extract_classification("Schinus molle")
col_extract_classification("Schinus molle", wide = TRUE)
col_extract_classification(c("Schinus molle", "Panthera leo"), wide = TRUE)

# Pass a match result data frame:
res <- col_match("Schinus molle")
col_extract_classification(res)
}
}
\seealso{
\code{\link[=clb_match]{clb_match()}}, \code{\link[=col_match]{col_match()}}, \code{\link[=clb_classification]{clb_classification()}}
}
