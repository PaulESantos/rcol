% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usage.R
\name{clb_distribution}
\alias{clb_distribution}
\alias{col_distribution}
\title{Geographic distribution for a taxon}
\usage{
clb_distribution(id, dataset = "3LXR", tidy = TRUE, full = FALSE, .raw = FALSE)

col_distribution(id, tidy = TRUE, full = FALSE, .raw = FALSE)
}
\arguments{
\item{id}{Taxon id (character), scientific name, or a data frame from \code{\link[=col_match]{col_match()}} or \code{\link[=col_check_name]{col_check_name()}}.}

\item{dataset}{Dataset key or alias. Defaults to \code{"3LXR"}.}

\item{tidy}{Logical. If \code{TRUE} (default), splits semicolon-delimited areas into distinct rows.}

\item{full}{Logical. If \code{FALSE} (default), returns only the essential \code{area} column. If \code{TRUE}, includes secondary metadata (\code{gazetteer}, \code{status}, \code{remarks}, \code{reference_id}).}

\item{.raw}{Return the raw parsed JSON instead of a tibble?}
}
\value{
A \link[tibble:tibble]{tibble} of distribution records.
}
\description{
Retrieves geographic distribution records (regions, countries) for a taxon.
By default, returns only the clean \code{area} column in tidy format.
}
\examples{
\dontrun{
# Distribution for Panthera leo (by ID or Scientific Name)
clb_distribution("4CGXP")
clb_distribution("Panthera leo")

# Distribution by passing col_check_name result directly
sp_info <- col_check_name("Schinus molle")
col_distribution(sp_info)
}
}
