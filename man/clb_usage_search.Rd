% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usage.R
\name{clb_usage_search}
\alias{clb_usage_search}
\alias{clb_search}
\title{Full-text search of name usages}
\usage{
clb_usage_search(
  q = NULL,
  dataset = "3LXR",
  rank = NULL,
  status = NULL,
  ...,
  limit = 50L,
  max = limit
)

clb_search(
  q = NULL,
  dataset = "3LXR",
  rank = NULL,
  status = NULL,
  ...,
  limit = 50L,
  max = limit
)
}
\arguments{
\item{q}{Free-text query. Optional (omit to browse with filters only).}

\item{dataset}{Dataset key or alias. Defaults to \code{"3LXR"}.}

\item{rank}{Filter by rank (e.g. \code{"species"}).}

\item{status}{Filter by taxonomic status (e.g. \code{"accepted"}, \code{"synonym"}).}

\item{...}{Further query parameters forwarded to the search endpoint
(e.g. \code{extinct}, \code{nomCode}, \code{minRank}, \code{maxRank}, \code{type}).}

\item{limit}{Page size per request.}

\item{max}{Maximum number of usages to return. Use \code{Inf} to fetch all.}
}
\value{
A \code{clb} object: a list with \verb{$data} (a \link[tibble:tibble]{tibble} of
usages) and \verb{$meta} (with \code{total}).
}
\description{
Searches name usages within a dataset (defaults to the latest extended COL
release). The accepted/synonym usage fields are hoisted to top-level columns;
the taxonomic classification and full name object are kept as list-columns.
}
\examples{
\dontrun{
clb_usage_search("Felidae")
clb_usage_search("Panthera", rank = "species", status = "accepted")
}
}
\seealso{
\code{\link[=clb_match]{clb_match()}}, \code{\link[=clb_suggest]{clb_suggest()}}, \code{\link[=clb_usage]{clb_usage()}}
}
