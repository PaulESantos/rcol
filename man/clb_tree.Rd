% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree.R
\name{clb_tree}
\alias{clb_tree}
\title{Navigate the taxonomic tree}
\usage{
clb_tree(
  dataset = "3LXR",
  id = NULL,
  extinct = TRUE,
  ...,
  limit = 100L,
  max = limit,
  .raw = FALSE
)
}
\arguments{
\item{dataset}{Dataset key or alias. Defaults to \code{"3LXR"}.}

\item{id}{Optional taxon id. When omitted the tree roots are returned.}

\item{extinct}{Logical; include extinct taxa. Defaults to \code{TRUE}.}

\item{...}{Further query parameters (e.g. \code{insertPlaceholder}).}

\item{limit}{Page size when listing roots.}

\item{max}{Maximum number of roots to return.}

\item{.raw}{Return the raw parsed JSON instead of a tibble?}
}
\value{
A \link[tibble:tibble]{tibble} of tree nodes with columns such as \code{id},
\code{name}, \code{authorship}, \code{rank}, \code{status}, \code{childCount} and \code{count}.
}
\description{
Without \code{id}, returns the root taxa of the dataset's tree. With \code{id},
returns the path (lineage) from the root down to that taxon. Use
\code{\link[=clb_children]{clb_children()}} to descend one level.
}
\examples{
\dontrun{
clb_tree(dataset = "3LR")            # kingdoms / roots
clb_tree(dataset = "3LR", id = "4CGXP")  # lineage of a taxon
}
}
\seealso{
\code{\link[=clb_children]{clb_children()}}, \code{\link[=clb_classification]{clb_classification()}}
}
