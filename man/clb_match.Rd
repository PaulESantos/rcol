% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{clb_match}
\alias{clb_match}
\title{Match a scientific name against a ChecklistBank dataset}
\usage{
clb_match(
  name,
  authorship = NULL,
  rank = NULL,
  code = NULL,
  dataset = "3LXR",
  server = NULL,
  .raw = FALSE
)
}
\arguments{
\item{name}{Scientific name to match. May include the authorship, or supply
it separately via \code{authorship}.}

\item{authorship}{Optional authorship string.}

\item{rank}{Optional rank to disambiguate (e.g. \code{"species"}, \code{"genus"}).}

\item{code}{Optional nomenclatural code (\code{"zoological"}, \code{"botanical"},
\code{"bacterial"}, \code{"virus"}, ...).}

\item{dataset}{Dataset key or alias to match against. Defaults to \code{"3LXR"}.
See \code{\link[=clb_col_release]{clb_col_release()}} for the COL release aliases.}

\item{server}{Optional base URL of an alternative matching service, e.g. a
locally running dockerized matching container
(\code{"http://localhost:8080"}). Overrides \code{CLB_BASE_URL} for this call.}

\item{.raw}{Return the raw parsed JSON response instead of a tibble?}
}
\value{
A one-row \link[tibble:tibble]{tibble} with the match outcome
(\code{match}, \code{match_type}), the matched usage (\code{usage_id}, \code{name},
\code{authorship}, \code{rank}, \code{status}, ...) and a \code{classification} list-column.
When nothing matches, \code{match} is \code{FALSE} and the usage columns are \code{NA}.
}
\description{
Looks up the single best-matching name usage for a scientific name in a
dataset, defaulting to the latest monthly extended Catalogue of Life release
(\code{"3LXR"}). This is the Catalogue of Life analogue of GBIF's name backbone
matching.
}
\examples{
\dontrun{
clb_match("Panthera leo")
clb_match("Abies alba", rank = "species", code = "botanical")
clb_match("Felis catus", dataset = "COL25")
}
}
\seealso{
\code{\link[=clb_match_verbose]{clb_match_verbose()}}, \code{\link[=clb_match_checklist]{clb_match_checklist()}}
}
