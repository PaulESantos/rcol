% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{clb_dataset_search}
\alias{clb_dataset_search}
\title{Search datasets in ChecklistBank}
\usage{
clb_dataset_search(
  q = NULL,
  type = NULL,
  origin = NULL,
  ...,
  limit = 50L,
  max = limit
)
}
\arguments{
\item{q}{Free-text query (matches title, alias, description, ...). Optional.}

\item{type}{Filter by dataset type (e.g. \code{"taxonomic"}, \code{"nomenclatural"}).}

\item{origin}{Filter by origin (e.g. \code{"external"}, \code{"project"}, \code{"release"},
\code{"xrelease"}).}

\item{...}{Further query parameters forwarded to the \verb{/dataset} endpoint
(e.g. \code{contributesTo}, \code{code}, \code{gbifKey}).}

\item{limit}{Page size per request.}

\item{max}{Maximum number of datasets to return. Use \code{Inf} to fetch all.}
}
\value{
A \link[tibble:tibble]{tibble} of datasets, one row each, with columns
such as \code{key}, \code{alias}, \code{title}, \code{type}, \code{origin}, \code{license} and \code{issued}.
}
\description{
Full-text and faceted search across all public datasets in ChecklistBank.
Uses the light dataset representation (\code{full = FALSE}) for speed.
}
\examples{
\dontrun{
clb_dataset_search("mammal")
clb_dataset_search(origin = "release", max = 1000)
}
}
\seealso{
\code{\link[=clb_dataset]{clb_dataset()}}, \code{\link[=clb_dataset_metrics]{clb_dataset_metrics()}}
}
