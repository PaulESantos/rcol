% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{clb_match_checklist}
\alias{clb_match_checklist}
\title{Match a checklist of names in bulk}
\usage{
clb_match_checklist(
  data,
  name = "name",
  authorship = "authorship",
  rank = "rank",
  code = "code",
  dataset = "3LXR",
  server = NULL,
  max_active = 5L
)
}
\arguments{
\item{data}{A character vector of names, or a data frame. For a data frame,
the columns named by \code{name}, \code{authorship}, \code{rank} and \code{code} are used
(missing ones are ignored).}

\item{name, authorship, rank, code}{Column names in \code{data} (when \code{data} is a
data frame) holding the respective fields.}

\item{dataset}{Dataset key or alias to match against. Defaults to \code{"3LXR"}.}

\item{server}{Optional alternative matching service base URL (see
\code{\link[=clb_match]{clb_match()}}).}

\item{max_active}{Maximum number of simultaneous HTTP requests.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per input, the \verb{verbatim_*}
input columns first followed by the match result columns of \code{\link[=clb_match]{clb_match()}}.
}
\description{
Matches many names at once, issuing requests in parallel. Input columns are
echoed back on each row with a \code{verbatim_} prefix so results stay aligned
with the source.
}
\examples{
\dontrun{
clb_match_checklist(c("Panthera leo", "Bufo bufo", "Abies alba"))

df <- data.frame(
  sciname = c("Puma concolor", "Vulpes vulpes"),
  rank = c("species", "species")
)
clb_match_checklist(df, name = "sciname", rank = "rank")
}
}
\seealso{
\code{\link[=clb_match]{clb_match()}}
}
