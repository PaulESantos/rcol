% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{clb_resolve_name}
\alias{clb_resolve_name}
\alias{col_resolve_name}
\alias{col_check_name}
\alias{clb_check_name}
\title{Resolve and verify valid species names}
\usage{
clb_resolve_name(
  name,
  authorship = NULL,
  rank = NULL,
  code = NULL,
  dataset = "3LXR",
  server = NULL
)

col_resolve_name(
  name,
  authorship = NULL,
  rank = NULL,
  code = NULL,
  server = NULL
)

col_check_name(
  name,
  authorship = NULL,
  rank = NULL,
  code = NULL,
  server = NULL
)

clb_check_name(
  name,
  authorship = NULL,
  rank = NULL,
  code = NULL,
  dataset = "3LXR",
  server = NULL
)
}
\arguments{
\item{name}{A scientific name string or character vector of scientific names.}

\item{authorship}{Optional authorship string.}

\item{rank}{Optional rank to disambiguate (e.g. \code{"species"}, \code{"genus"}).}

\item{code}{Optional nomenclatural code (\code{"zoological"}, \code{"botanical"},
\code{"bacterial"}, \code{"virus"}, ...).}

\item{dataset}{Dataset key or alias to match against. Defaults to \code{"3LXR"}.
See \code{\link[=clb_col_release]{clb_col_release()}} for the COL release aliases.}

\item{server}{Optional base URL of an alternative matching service, e.g. a
locally running dockerized matching container
(\code{"http://localhost:8080"}). Overrides \code{CLB_BASE_URL} for this call.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with columns:
\item{queried_name}{The original scientific name queried.}
\item{match}{Logical indicator of whether a match was found.}
\item{match_type}{Type of match (\code{"exact"}, \code{"variant"}, \code{"fuzzy"}, etc.).}
\item{status}{Taxonomic status of the queried name (\code{"accepted"}, \code{"synonym"}).}
\item{matched_name}{Scientific name returned by the match service.}
\item{accepted_name}{The valid/accepted scientific name.}
\item{accepted_authorship}{Authorship of the accepted scientific name.}
\item{accepted_rank}{Taxonomic rank of the accepted name.}
\item{accepted_id}{ChecklistBank usage ID of the accepted name.}
}
\description{
Provides a clean, simplified verification table for one or more scientific
names. Unlike \code{\link[=col_match]{col_match()}}, which returns full raw API columns and list
columns, \code{col_resolve_name()} returns only the essential fields needed to
verify whether a name is accepted or a synonym, and what its accepted name is.
}
\examples{
\dontrun{
# Resolve a single name
col_resolve_name("Werneria nubigena")

# Check multiple names at once
col_check_name(c("Werneria nubigena", "Panthera leo", "Schinus molle"))
}
}
