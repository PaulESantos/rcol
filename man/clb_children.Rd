% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree.R
\name{clb_children}
\alias{clb_children}
\title{Direct children of a taxon}
\usage{
clb_children(
  id,
  dataset = "3LXR",
  extinct = TRUE,
  ...,
  limit = 100L,
  max = limit,
  .raw = FALSE
)
}
\arguments{
\item{id}{Taxon id (character), scientific name, or a data frame from \code{\link[=col_match]{col_match()}} or \code{\link[=col_check_name]{col_check_name()}}.}

\item{dataset}{Dataset key or alias. Defaults to \code{"3LXR"}.}

\item{extinct}{Logical; include extinct taxa. Defaults to \code{TRUE}.}

\item{...}{Further query parameters (e.g. \code{insertPlaceholder}).}

\item{limit}{Page size per request.}

\item{max}{Maximum number of children to return. Use \code{Inf} to fetch all.}

\item{.raw}{Return the raw parsed JSON instead of a tibble?}
}
\value{
A \link[tibble:tibble]{tibble} of child tree nodes.
}
\description{
Lists the immediate child taxa of a node in the taxonomic tree.
Accepts a taxon ID, a scientific name, or a data frame from \code{\link[=col_check_name]{col_check_name()}} or \code{\link[=col_match]{col_match()}}.
}
\examples{
\dontrun{
roots <- clb_tree(dataset = "3LR")
clb_children(roots$id[1], dataset = "3LR")
clb_children("Panthera")
}
}
\seealso{
\code{\link[=clb_tree]{clb_tree()}}, \code{\link[=clb_classification]{clb_classification()}}
}
