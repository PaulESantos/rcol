---
title: "Manual Integrado de Flujo de Trabajo y Funciones del Paquete `rcol`"
subtitle: "Arquitectura de API, Dependencias, Entradas y Salidas de las 46 Funciones Públicas"
author: "Equipo de Desarrollo rcol"
date: today
format:
  html:
    toc: true
    toc-depth: 3
    toc-title: "Índice de Contenido"
    number-sections: true
    code-fold: show
    theme: flatly
---

## Introducción y Arquitectura General

El paquete **`rcol`** (*R Client for the Catalogue of Life / ChecklistBank API*) proporciona un acceso idiomático y de alto rendimiento a la API REST de **ChecklistBank** (`https://api.checklistbank.org`) y a las publicaciones mensuales y anuales del **Catalogue of Life (COL)**.

El paquete exporta **46 funciones públicas** organizadas en dos capas o familias principales de funciones:

1. **Familia `clb_*()` (Bajo Nivel / Universal):**
   - Requieren o aceptan explícitamente el parámetro `dataset` (`dataset = "3LXR"`, `dataset = "COL25"`, `dataset = 2099`, etc.).
   - Permiten consultar cualquier conjunto de datos publicado en ChecklistBank o versiones históricas del COL.

2. **Familia `col_*()` (Atajos / Pinned Version):**
   - Omiten el argumento `dataset`.
   - Utilizan internamente la función `col_key()` para fijar la versión extendida del Catalogue of Life en la sesión activa (`"3LXR"`).
   - Garantizan consistencia en análisis científicos al fijar la clave numérica en la sesión.

---

### Conceptos Clave y Estandarización de Funciones

#### 1. Diferencia entre `clb_match()` y `col_match()`
- **`clb_match()`**: Es el **motor base de bajo nivel** del sistema de coincidencia de nombres científicos. Consulta directamente ChecklistBank y permite especificar cualquier `dataset` (versiones históricas del COL, catálogos locales, etc.).
- **`col_match()`**: Es la **función atajo de alto nivel** predeterminada para trabajar con el Catalogue of Life. Es un wrapper de `clb_match()` que utiliza automáticamente la clave del release extendido de COL fijado en la sesión activa (`col_key()`).

#### 2. `col_match()` vs. `col_check_name()` / `col_resolve_name()`
- **`col_match()`**: Recupera la **información taxonómica cruda completa** directamente desde la API (16 columnas, incluyendo la lista anidada `classification`, metadatos nomenclaturales, etc.). Es ideal para análisis exhaustivos.
- **`col_check_name()` / `col_resolve_name()`**: Organizan y simplifican los resultados en un `tibble` limpio de 9 columnas esenciales. Muestran de forma rápida si el nombre es **aceptado o sinónimo**, el **nombre aceptado válido**, la autoría y su ID.

#### 3. Estandarización de `col_classification()` y `clb_classification()`
- `col_classification()` y `clb_classification()` se han unificado para permitir:
  - Búsqueda por **ID de taxón** (ej. `"4V8GC"`).
  - Búsqueda por **nombre científico directo** (ej. `"Schinus molle"`).
  - Consulta desde un **dataframe de match** (`col_check_name()` o `col_match()`).
  - Soporte del parámetro `wide = TRUE` / `wide = FALSE` para obtener la jerarquía desanidada en formato Ancho o Largo (manteniendo `col_extract_classification()` como alias totalmente compatible).

#### 4. Flexibilidad de Entradas en el Módulo de Relaciones
Todas las funciones del módulo de relaciones (`col_synonyms()`, `col_vernacular()`, `col_distribution()`, `col_classification()`, `col_usage()`, `col_usage_metrics()`) aceptan indistintamente cualquiera de los siguientes tres tipos de entrada:
1. **Un nombre científico directo** (ej. `"Werneria nubigena"`, `"Panthera leo"`).
2. **Un ID alfanumérico del COL** (ej. `"B87CR"`, `"4CGXP"`).
3. **El dataframe completo devuelto por `col_check_name()` o `col_match()`**.

---

## Diagrama de Flujo de Dependencias e Interacciones

El siguiente **Diagrama Interactivo Dinámico** permite explorar la arquitectura de llamadas, herencia de dataframes y flujo de dependencias entre las funciones del paquete `rcol`. 

:::{.callout-note}
💡 **Instrucciones de interacción:**
- **Navegación:** Usa la rueda del ratón o los botones flotantes (+ / -) para acercar/alejar y arrastra el lienzo para moverte libremente.
- **Mover nodos:** Puedes hacer clic y arrastrar cualquier nodo para acomodarlo a tu gusto.
- **Tooltip de información:** Pasa el cursor sobre cualquier nodo para ver una descripción detallada.
:::

```{=html}
<script type="text/javascript" src="https://unpkg.com/vis-network/standalone/umd/vis-network.min.js"></script>
<style>
  #rcol-network-container {
    width: 100%;
    height: 650px;
    border: 1px solid #dee2e6;
    border-radius: 10px;
    background-color: #ffffff;
    box-shadow: 0 4px 15px rgba(0,0,0,0.06);
    margin-top: 15px;
    margin-bottom: 25px;
  }
  .rcol-legend {
    display: flex;
    justify-content: center;
    gap: 15px;
    margin-top: 10px;
    margin-bottom: 10px;
    flex-wrap: wrap;
    font-family: inherit;
  }
  .rcol-legend-item {
    display: flex;
    align-items: center;
    font-size: 13px;
    font-weight: 600;
    color: #495057;
  }
  .rcol-legend-color {
    width: 16px;
    height: 16px;
    border-radius: 4px;
    margin-right: 6px;
    border: 1px solid rgba(0,0,0,0.15);
  }
</style>

<div class="rcol-legend">
  <div class="rcol-legend-item"><div class="rcol-legend-color" style="background:#e3f2fd; border-color:#1565c0;"></div> 1. Configuración & Sesión</div>
  <div class="rcol-legend-item"><div class="rcol-legend-color" style="background:#e8f5e9; border-color:#2e7d32;"></div> 2. Matching & Verificación</div>
  <div class="rcol-legend-item"><div class="rcol-legend-color" style="background:#fff3e0; border-color:#e65100;"></div> 3. Extracción Clasificación</div>
  <div class="rcol-legend-item"><div class="rcol-legend-color" style="background:#f3e5f5; border-color:#7b1fa2;"></div> 4. Relaciones Taxonómicas</div>
  <div class="rcol-legend-item"><div class="rcol-legend-color" style="background:#eceff1; border-color:#455a64;"></div> 5. Árbol Taxonómico</div>
</div>

<div id="rcol-network-container"></div>

<script>
document.addEventListener("DOMContentLoaded", function() {
  var container = document.getElementById('rcol-network-container');
  if (!container) return;

  var nodes = new vis.DataSet([
    // 1. Configuración & Sesión
    { id: 'base_url', label: 'clb_base_url()', group: 'config', title: 'URL base de ChecklistBank' },
    { id: 'key', label: 'col_key()', group: 'config', shape: 'ellipse', title: 'Fija versión activa COL (3LXR)' },
    { id: 'refresh', label: 'col_refresh()', group: 'config', title: 'Refresca clave de versión' },

    // 2. Matching & Verificación
    { id: 'check_name', label: 'col_check_name()\ncol_resolve_name()', group: 'match', shape: 'box', font: { size: 14, bold: true }, title: 'Verificación limpia de nombres (9 columnas)' },
    { id: 'col_match', label: 'col_match()', group: 'match', font: { size: 13, bold: true }, title: 'Matching de 1 nombre científico en COL' },
    { id: 'clb_match', label: 'clb_match()', group: 'match', title: 'Matching universal en ChecklistBank' },
    { id: 'match_chk', label: 'col_match_checklist()', group: 'match', title: 'Matching masivo en paralelo' },
    { id: 'clb_chk', label: 'clb_match_checklist()', group: 'match', title: 'Matching masivo universal' },
    { id: 'match_verb', label: 'col_match_verbose()', group: 'match', title: 'Matching con lista de homónimos' },

    // 3. Extracción Clasificación
    { id: 'col_ext', label: 'col_extract_classification()', group: 'extract', shape: 'box', font: { size: 14, bold: true }, title: 'Extrae jerarquía a formato Ancho o Largo' },
    { id: 'clb_ext', label: 'clb_extract_classification()', group: 'extract', title: 'Extrae jerarquía universal' },

    // 4. Relaciones Taxonómicas
    { id: 'col_syn', label: 'col_synonyms()', group: 'rel', font: { size: 13, bold: true }, title: 'Sinónimos etiquetados (acepta df de match)' },
    { id: 'clb_syn', label: 'clb_synonyms()', group: 'rel', title: 'Sinónimos universal' },
    { id: 'col_vern', label: 'col_vernacular()', group: 'rel', font: { size: 13, bold: true }, title: 'Nombres comunes depurados (acepta df de match)' },
    { id: 'clb_vern', label: 'clb_vernacular()', group: 'rel', title: 'Nombres comunes universal' },
    { id: 'col_dist', label: 'col_distribution()', group: 'rel', shape: 'box', font: { size: 14, bold: true }, title: 'Distribución geográfica tidy (columna area)' },
    { id: 'clb_dist', label: 'clb_distribution()', group: 'rel', title: 'Distribución geográfica universal' },
    { id: 'col_usage', label: 'col_usage()', group: 'rel', title: 'Detalles de uso taxonómico' },
    { id: 'clb_usage', label: 'clb_usage()', group: 'rel', title: 'Detalles universal' },

    // 5. Árbol Taxonómico
    { id: 'col_tree', label: 'col_tree()', group: 'tree', title: 'Nodos del árbol taxonómico' },
    { id: 'clb_tree', label: 'clb_tree()', group: 'tree', title: 'Árbol universal' },
    { id: 'col_child', label: 'col_children()', group: 'tree', title: 'Hijos directos del taxón' },
    { id: 'clb_child', label: 'clb_children()', group: 'tree', title: 'Hijos directos universal' }
  ]);

  var edges = new vis.DataSet([
    // Configuración
    { from: 'key', to: 'col_match', label: 'pin versión', dashes: true, color: { color: '#1565c0' } },
    { from: 'key', to: 'match_chk', label: 'pin versión', dashes: true, color: { color: '#1565c0' } },
    { from: 'key', to: 'col_dist', label: 'pin versión', dashes: true, color: { color: '#1565c0' } },
    { from: 'key', to: 'refresh', label: 'refresca' },

    // Matching
    { from: 'check_name', to: 'col_match', label: '1 nombre', width: 2 },
    { from: 'check_name', to: 'match_chk', label: 'varios nombres', width: 2 },
    { from: 'col_match', to: 'clb_match', label: 'delega' },
    { from: 'match_chk', to: 'clb_chk', label: 'delega' },
    { from: 'match_verb', to: 'clb_match_verbose', label: 'delega' },

    // Flujo de Dataframe
    { from: 'check_name', to: 'col_ext', label: 'pasa dataframe', color: { color: '#e65100' }, width: 2.5 },
    { from: 'check_name', to: 'col_syn', label: 'pasa dataframe', color: { color: '#7b1fa2' }, width: 2.5 },
    { from: 'check_name', to: 'col_vern', label: 'pasa dataframe', color: { color: '#7b1fa2' }, width: 2.5 },
    { from: 'check_name', to: 'col_dist', label: 'pasa dataframe', color: { color: '#7b1fa2' }, width: 2.5 },

    { from: 'col_match', to: 'col_ext', label: 'pasa dataframe', color: { color: '#e65100' } },
    { from: 'col_match', to: 'col_syn', label: 'pasa dataframe', color: { color: '#7b1fa2' } },
    { from: 'col_match', to: 'col_vern', label: 'pasa dataframe', color: { color: '#7b1fa2' } },
    { from: 'col_match', to: 'col_dist', label: 'pasa dataframe', color: { color: '#7b1fa2' } },

    // Delegación alto -> bajo nivel
    { from: 'col_ext', to: 'clb_ext', label: 'delega' },
    { from: 'col_syn', to: 'clb_syn', label: 'delega' },
    { from: 'col_vern', to: 'clb_vern', label: 'delega' },
    { from: 'col_dist', to: 'clb_dist', label: 'delega' },
    { from: 'col_usage', to: 'clb_usage', label: 'delega' },
    { from: 'col_tree', to: 'clb_tree', label: 'delega' },
    { from: 'col_child', to: 'clb_child', label: 'delega' }
  ]);

  var data = { nodes: nodes, edges: edges };

  var options = {
    groups: {
      config: { color: { background: '#e3f2fd', border: '#1565c0' }, font: { color: '#0d47a1', face: 'Segoe UI' } },
      match: { color: { background: '#e8f5e9', border: '#2e7d32' }, font: { color: '#1b5e20', face: 'Segoe UI' } },
      extract: { color: { background: '#fff3e0', border: '#e65100' }, font: { color: '#bf360c', face: 'Segoe UI' } },
      rel: { color: { background: '#f3e5f5', border: '#7b1fa2' }, font: { color: '#4a148c', face: 'Segoe UI' } },
      tree: { color: { background: '#eceff1', border: '#455a64' }, font: { color: '#263238', face: 'Segoe UI' } }
    },
    nodes: {
      shape: 'box',
      margin: 12,
      shadow: { enabled: true, color: 'rgba(0,0,0,0.08)', size: 6, x: 2, y: 2 },
      font: { face: 'Segoe UI', size: 12 }
    },
    edges: {
      arrows: { to: { enabled: true, scaleFactor: 0.8 } },
      smooth: { type: 'cubicBezier', forceDirection: 'vertical', roundness: 0.4 },
      font: { size: 10, align: 'horizontal', face: 'Segoe UI' }
    },
    physics: {
      solver: 'hierarchicalRepulsion',
      hierarchicalRepulsion: {
        centralGravity: 0.0,
        springLength: 120,
        springConstant: 0.05,
        nodeDistance: 160,
        damping: 0.09
      },
      stabilization: { iterations: 200 }
    },
    interaction: {
      hover: true,
      tooltipDelay: 100,
      zoomView: true,
      dragView: true,
      navigationButtons: true
    }
  };

  var network = new vis.Network(container, data, options);
});
</script>
```

---

## Especificación de Funciones por Módulo

### 1. Configuración de Entorno y Pinning de Versión

#### `clb_base_url()`
- **Descripción:** Obtiene la URL base de la API de ChecklistBank.
- **Entradas (`Inputs`):** Ninguna (lee la variable de entorno `CLB_BASE_URL` o usa `"https://api.checklistbank.org"`).
- **Salida (`Output`):** Vector de caracteres de longitud 1 (`character`).

#### `col_key(refresh = FALSE)`
- **Descripción:** Resuelve la clave numérica de la versión extendida de COL (`"3LXR"`) y la almacena en caché de sesión.
- **Entradas (`Inputs`):** `refresh` (`logical`, por defecto `FALSE`).
- **Salida (`Output`):** Vector entero (`integer` key, ej. `3`).

#### `col_refresh()`
- **Descripción:** Fuerza la re-resolución y actualización de la versión del COL fijada en sesión.
- **Entradas (`Inputs`):** Ninguna.
- **Salida (`Output`):** Vector entero (`integer`), de forma invisible.

#### `clb_col_release(cadence = c("monthly", "annual"), extended = FALSE)`
- **Descripción:** Obtiene el alias estándar del release de COL (ej. `"3LR"`, `"3LXR"`, `"COL25"`).
- **Entradas (`Inputs`):** `cadence` (`"monthly"` o `"annual"`), `extended` (`logical`).
- **Salida (`Output`):** Vector de caracteres (`character`, ej. `"3LXR"`).

#### `clb_col_releases(project = 3L, ...)`
- **Descripción:** Lista todas las versiones históricas anuales y mensuales derivadas del proyecto COL (`project = 3`).
- **Entradas (`Inputs`):** `project` (`integer`), parámetros adicionales.
- **Salida (`Output`):** `tibble` con columnas `key`, `alias`, `title`, `issued`, `version`, etc.

---

### 2. Coincidencia Taxonómica (*Name Matching*) y Verificación

#### `clb_resolve_name(name, ...)` / `clb_check_name(name, ...)` / `col_resolve_name(name, ...)` / `col_check_name(name, ...)`
- **Descripción:** Resolución limpia y simplificada de alta legibilidad para verificar directamente el nombre válido/aceptado de una especie o lista de especies. Elimina la complejidad de metadatos crudos y listas anidadas de `col_match()`.
- **Entradas (`Inputs`):** `name` (cadena de texto simple o vector de caracteres).
- **Salida (`Output`):** `tibble` compacto con 9 columnas esenciales en orden de lectura: `queried_name`, `matched_name`, `status`, `accepted_name`, `accepted_authorship`, `accepted_rank`, `accepted_id`, `match`, `match_type`.
- **Dependencias:** Llama internamente a `col_match()` para 1 especie o `col_match_checklist()` para vectores de especies.

#### `clb_match(name, authorship = NULL, rank = NULL, code = NULL, dataset = "3LXR", server = NULL, .raw = FALSE)` / `col_match(...)`
- **Descripción:** Busca la única mejor coincidencia para un nombre científico en un dataset.
- **Entradas (`Inputs`):** `name` (`character`), `authorship`, `rank`, `code`, `dataset`, `server`, `.raw`.
- **Salida (`Output`):** `tibble` de **1 fila × 16 columnas** (`match`, `match_type`, `usage_id`, `name`, `authorship`, `rank`, `status`, `accepted_id`, `accepted_name`, `accepted_authorship`, `code`, `label`, `parent_id`, `names_index_id`, `names_index_match_type`, `classification`). Resuelve automáticamente el taxón aceptado (`accepted_*`) tanto para nombres aceptados como para sinónimos.

#### `clb_match_verbose(...)` / `col_match_verbose(...)`
- **Descripción:** Retorna la coincidencia principal junto con todos los homónimos o candidatos alternativos.
- **Salida (`Output`):** `tibble` de N filas con una columna lógica `primary` (`TRUE` para la coincidencia elegida, `FALSE` para las alternativas).

#### `clb_match_checklist(data, name = "name", authorship = "authorship", rank = "rank", code = "code", dataset = "3LXR", server = NULL, max_active = 5L)` / `col_match_checklist(...)`
- **Descripción:** Realiza el emparejamiento masivo de una lista o data frame de nombres usando solicitudes HTTP en paralelo (`httr2::req_perform_parallel`).
- **Salida (`Output`):** `tibble` con las columnas originales de entrada prefijadas con `verbatim_*` seguidas de las columnas de resultado de match.

---

### 3. Extracción de Clasificación Jerárquica

#### `clb_extract_classification(data, wide = FALSE, dataset = "3LXR", ...)` / `col_extract_classification(data, wide = FALSE, ...)`
- **Descripción:** Desanida y extrae la jerarquía taxonómica completa a una tabla limpia en formato Largo o Ancho (`wide = TRUE`). Admite vectores de nombres o dataframes devueltos por `col_check_name()` o `col_match()`.
- **Entradas (`Inputs`):** `data` (vector de caracteres o dataframe de match), `wide` (`logical`, `FALSE` para formato largo, `TRUE` para formato ancho con columnas `kingdom`, `phylum`, `class`, `order`, `family`, `genus`, `species`).
- **Salida (`Output`):** `tibble` estructurado con la clasificación completa.

---

### 4. Consultas de Usos Taxonómicos y Búsqueda Libre

#### `clb_usage(id, dataset = "3LXR", .raw = FALSE)` / `col_usage(id, .raw = FALSE)`
- **Descripción:** Obtiene los detalles completos de un uso taxonómico (especie o sinónimo) por su ID único.
- **Salida (`Output`):** `tibble` de **1 fila** (`id`, `scientific_name`, `authorship`, `rank`, `status`, `label`, `parent_id`, `extinct`, `name`).

#### `clb_usage_search(q = NULL, dataset = "3LXR", rank = NULL, status = NULL, ..., limit = 50L, max = limit)` / `col_usage_search(...)`
- **Aliases:** `clb_search()`, `col_search()`.
- **Descripción:** Búsqueda por texto libre de usos taxonómicos en un dataset.
- **Salida (`Output`):** Objeto S3 `clb` con `$data` (`tibble` de resultados) y `$meta` (total de registros).

#### `clb_suggest(q, dataset = "3LXR", ..., .raw = FALSE)` / `col_suggest(...)`
- **Descripción:** Autocompletado rápido de nombres por prefijo.
- **Salida (`Output`):** `tibble` con sugerencias rápidas (`suggestion`, `usageId`, `rank`, `status`).

---

### 5. Relaciones Taxonómicas: Jerarquía, Sinónimos, Nombres Comunes, Distribución y Métricas

#### `clb_classification(id, dataset = "3LXR", wide = FALSE, .raw = FALSE)` / `col_classification(...)`
- **Descripción:** Retorna la cadena ordenada de ancestros de un taxón por su nombre científico directo, ID o dataframe de match.
- **Salida (`Output`):** `tibble` de ancestros (1 fila por rango) con columnas limpias: `id`, `scientific_name`, `authorship`, `rank`, `full_name` (sin código HTML de cursiva).

#### `clb_synonyms(id, dataset = "3LXR", full = FALSE, .raw = FALSE)` / `col_synonyms(id, full = FALSE, .raw = FALSE)`
- **Descripción:** Obtiene todos los sinónimos asociados a un taxón. Acepta un **nombre científico directo**, un **ID de taxón**, o un **dataframe devuelto por `col_check_name()` o `col_match()`**. Por defecto retorna únicamente las 6 columnas esenciales (`accepted_name`, `scientific_name`, `authorship`, `synonym_type`, `rank`, `full_name`).
- **Entradas (`Inputs`):** `id` (nombre científico, ID o dataframe de match), `full` (`logical`, por defecto `FALSE`).
- **Salida (`Output`):** `tibble` limpio con 6 columnas esenciales (o metadatos secundarios completos si `full = TRUE`).

#### `clb_vernacular(id = NULL, dataset = "3LXR", q = NULL, lang = NULL, ..., limit = 50L, max = limit, full = FALSE)` / `col_vernacular(...)`
- **Descripción:** Obtiene los nombres comunes o vernáculos por nombre científico directo, vector de múltiples especies, ID de taxón o dataframe de match.
- **Salida (`Output`):** `tibble` depurado que preserva la asociación de especies mediante la columna `scientific_name` (`scientific_name`, `name`, `language`, `latin`, `country`, `area`, `remarks`). Excluye `referenceId` y metadatos no esenciales por defecto.

#### `clb_distribution(id, dataset = "3LXR", tidy = TRUE, full = FALSE, .raw = FALSE)` / `col_distribution(...)`
- **Descripción:** Obtiene la distribución geográfica oficial (países y regiones) para un taxón por su nombre científico directo, vector de especies, ID o dataframe de match.
- **Entradas (`Inputs`):** `id` (cadena, vector o dataframe de match), `tidy` (`logical`, por defecto `TRUE`, desglosa regiones delimitadas por punto y coma en filas individuales), `full` (`logical`, por defecto `FALSE`).
- **Salida (`Output`):** `tibble` depurado con la columna `area` (y `scientific_name` cuando se consultan múltiples especies).

#### `clb_usage_metrics(id, dataset = "3LXR", .raw = FALSE)` / `col_usage_metrics(...)`
- **Descripción:** Obtiene métricas del subárbol de un taxón (conteo de especies descendientes, desglose por rango).
- **Salida (`Output`):** `tibble` de 1 fila con métricas y mapa de recuentos.

---

### 6. Navegación del Árbol Taxonómico

#### `clb_tree(dataset = "3LXR", id = NULL, extinct = TRUE, ..., limit = 100L, max = limit)` / `col_tree(...)`
- **Descripción:** Retorna los nodos raíz del árbol taxonómico o los hijos directos de un nodo.
- **Salida (`Output`):** `tibble` de nodos del árbol (`id`, `name`, `rank`, `childCount`).

#### `clb_children(id, dataset = "3LXR", extinct = TRUE, ..., limit = 100L, max = limit)` / `col_children(...)`
- **Descripción:** Retorna los taxones hijos inmediatos de un taxón padre.
- **Salida (`Output`):** `tibble` de taxones hijos.

---

### 7. Catálogo de Datasets

#### `clb_dataset_search(q = NULL, ...)`
- **Descripción:** Busca conjuntos de datos dentro de ChecklistBank.
- **Salida (`Output`):** Objeto S3 `clb` de datasets.

#### `clb_dataset(key, .raw = FALSE)` / `col_dataset(...)`
- **Descripción:** Obtiene los metadatos completos de un conjunto de datos por su clave o alias.
- **Salida (`Output`):** `tibble` de 1 fila con metadatos del dataset (`title`, `version`, `publisher`, etc.).

#### `clb_dataset_metrics(key, .raw = FALSE)` / `col_dataset_metrics(...)`
- **Descripción:** Métricas globales de un dataset (total de taxones, sinónimos, distribución de rangos).
- **Salida (`Output`):** `tibble` de métricas del dataset.

---

### 8. Parsers Nomenclaturales y de Valores

#### `clb_parse_name(names, .raw = FALSE)`
- **Descripción:** Parsea nombres científicos en sus componentes atómicos (epíteto específico, autoría, género, subgénero).
- **Salida (`Output`):** `tibble` estructurado del parser taxonómico.

#### `clb_parsers()`
- **Descripción:** Lista todos los tipos de parsers de valores controlados disponibles en ChecklistBank.
- **Salida (`Output`):** Vector de caracteres con los nombres de los parsers.

#### `clb_parse(type, values, .raw = FALSE)`
- **Descripción:** Parsea valores utilizando un parser controlado específico (ej. `rank`, `nomStatus`).
- **Salida (`Output`):** `tibble` con los valores mapeados al estándar.

---

## Ejemplo Completo de Flujo de Trabajo en R

```r
library(rcol)
library(dplyr)

# 1. Fijar o verificar la versión activa del Catalogue of Life
col_key()

# 2. Verificación limpia de nombres válidos/aceptados (col_check_name)
especies <- c("Werneria nubigena", "Panthera leo", "Schinus molle")
resumen_nombres <- col_check_name(especies)
print(resumen_nombres)

# 3. Extracción de clasificación taxonómica jerárquica en formato Ancho
clasif_wide <- col_extract_classification(resumen_nombres, wide = TRUE)
print(clasif_wide)

# 4. Obtención de sinónimos taxonómicos pasando directamente el dataframe de match
sinonimos_werneria <- col_synonyms(resumen_nombres[1, ])
print(sinonimos_werneria)

# 5. Obtención de nombres vernáculos depurados
nombres_comunes_leo <- col_vernacular(resumen_nombres[2, ])
print(nombres_comunes_leo)

# 6. Obtención de distribución geográfica en formato tidy (columna area)
distribucion_schinus <- col_distribution(resumen_nombres[3, ])
print(distribucion_schinus)

# 7. Parsing anatómico de nombres científicos
parsed <- clb_parse_name("Abies alba Mill. var. alpina")
print(parsed)
```

---

## Verificación de Calidad y Cumplimiento de Estándares CRAN

Se ha llevado a cabo una auditoría exhaustiva siguiendo los lineamientos de la skill **`cran-extrachecks`** y la revisión técnica de paquetes en R:

### 1. Estado de `R CMD check`
- **Resultado:** `0 ERRORS | 0 WARNINGS | 0 NOTES`.
- **Compatibilidad R:** R 4.1+ en plataformas Windows, macOS y Linux.
- **Suite de Pruebas Unitarias (`testthat` edición 3):** 117 pruebas unitarias ejecutadas con resultado **100% PASS** (`117 PASS | 0 FAIL | 0 WARN`).

### 2. Cumplimiento de Requisitos CRAN Extra-Checks
- **`DESCRIPTION`**:
  - `Title`: En formato Title Case, citando correctamente las librerías entre comillas (`'rgbif'`, `'taxadb'`).
  - `Description`: Explicación desarrollada en múltiples oraciones, sin frases prohibidas.
  - `Authors@R`: Incluye el rol de titular de derechos de autor `[cph]`.
  - `LICENSE`: Año actualizado a **2026**.
- **Documentación de Funciones**:
  - Todas las 46 funciones exportadas cuentan con etiqueta `@return` detallada.
  - Todos los argumentos expuestos (`wide`, `full`, `id`, `dataset`, etc.) están completamente documentados en los archivos `.Rd`.
- **Archivos Administrativos**:
  - `NEWS.md`: Presente y actualizado con el registro de cambios de la versión `0.0.1`.
  - `cran-comments.md`: Creado y preparado para el proceso de envío a CRAN.

